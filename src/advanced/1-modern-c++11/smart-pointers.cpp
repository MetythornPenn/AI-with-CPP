#include <iostream>
// #include <make_unique>

/*
Smart Pointers (std::unique_ptr, std::shared_ptr):
Automate memory management, prevent memory leaks.

*/
int main() {

    // auto ptr = std::make_unique<int>(10);
    // std::cout << *ptr << "\n";


}
